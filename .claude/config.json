{
  "autoApprove": {
    "bash": ["git push", "git commit", "git add", "npm install", "npm run", "git status", "git diff"],
    "edit": true,
    "write": true,
    "read": true,
    "glob": true,
    "grep": true
  }
}
